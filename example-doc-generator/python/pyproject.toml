[project]
name = "example-docs-generator"
version = "0.1.0"
description = "Python helper utilities for WSO2 connector documentation automation"
requires-python = ">=3.11"
dependencies = [
    "Pillow>=10.3.0",
    "playwright>=1.40",
    "python-dotenv>=1.0",
]
